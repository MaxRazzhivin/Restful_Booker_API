# 🧪 RESTful Booker API Tests

> **Fullstack QA Project**: автоматизация API-тестов с использованием **Python + Pytest + Allure + Docker + GitHub Actions CI/CD**.  
> Полный цикл — от локального запуска до автоматического отчёта на GitHub Pages.

```
В проекте используется публичный сервис RESTful Booker￼(https://restful-booker.herokuapp.com/apidoc/index.html#api-Booking)
Реализованы тесты полного цикла CRUD (создание, чтение, обновление, удаление) с позитивными и негативными сценариями.
В фикстурах настроено:
	•	создание авторизованной сессии,
	•	генерация тестовых данных с помощью библиотеки Faker.

```

---

## 🚀 Стек технологий

| Компонент        | Назначение |
|------------------|-------------|
| ***Python 3.11*** | Язык для написания тестов |
| ***Pytest***          | Фреймворк для автотестов |
| **Allure**       | Генерация детализированных HTML-отчётов |
| **Docker**       | Изолированная среда для выполнения тестов |
| **GitHub Actions** | CI/CD: автоматический прогон и деплой отчётов |
| **GitHub Pages** | Публикация отчёта в виде сайта |

---

---

## 🧰 Установка и запуск локально (через Docker)

> Все тесты запускаются в контейнере, без установки зависимостей на твою систему.

```bash
# Сборка Docker-образа
docker build -t restful-tests .

# Запуск тестов внутри контейнера
docker run --rm restful-tests

# Если хочешь сохранить Allure-отчёт локально на диск:
docker run --rm -v $(pwd)/reports:/app/reports restful-tests

```

## ⚙️ CI/CD пайплайн (GitHub Actions)
```
CI запускается автоматически при каждом push в ветку main:
	1.	Устанавливает зависимости
	2.	Собирает Docker-образ
	3.	Запускает тесты (pytest)
	4.	Генерирует Allure-отчёт
	5.	Публикует HTML-отчёт на GitHub Pages

🔗 Результаты прогона:
	✅ CI-проверка 
    📊 Allure-отчёт: https://maxrazzhivin.github.io/Restful_Booker_API/
```

## 🧾 Дополнительно
	Все тесты снабжены шагами Allure (@allure.step)
	Настройка логирования в pytest.ini
	Конфигурация GitHub Actions находится в .github/workflows/tests.yml
	Отчёты публикуются автоматически в ветку gh-pages



